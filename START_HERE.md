# START_HERE — THE_SYSTEM_PACK v1 (2026-07-27)
One pack, three prior zips deduplicated, all tables parquet. The complete carried
history of the program plus the new project founding docs.

## READ ORDER FOR A FRESH CHAT (and nothing more)
1. docs/newproject/PROJECT_INSTRUCTIONS.md — the ten rules. Binding.
2. docs/newproject/COMPONENTS.md — what every component is.
3. docs/newproject/FIDELITY_AUDIT_2026-07-27.md — how the old program diverged from the ask.
4. docs/newproject/PACK3_INSIGHTS_2026-07-27.md — level-map recoveries and corrections.
Then open ONLY what the current task needs. MANIFEST.md maps every file.

## THE READ-GATE LAW (token discipline)
- data/ is read by CODE only — loaded, queried, counted. Never printed into chat.
- archive/ is NEVER opened unprompted. It exists so nothing is lost, not to be read.
- docs/program, docs/levelmap, docs/lawlayer, docs/optionb are reference: open per
  task, never wholesale. The four newproject docs above are the only default reads.
- Files tagged UNREAD in MANIFEST.md have never been read by any AI in this lineage:
  read them before relying on them.

## WHAT LIVES WHERE
docs/     program (bruteforce / Map v3 era) · levelmap (M-program: contracts, Book,
          registry, reproduction guide, verify harness) · lawlayer (master reference,
          manifests, verdicts, bootstraps) · optionb (rulebooks v1.0-1.2) · newproject
code/     bruteforce pipeline · levelmap Pine (CB9/CB10) · pipeline (fetch_arch) ·
          runner (GitHub workflows)
data/     raw (FROZEN 5m vintage 2021-09 to 2026-07-06 — the level-map source data) ·
          events (p2 x4, p1, level_events_v4) · state (M1_state, vantage basis,
          component series, dial tables) · results (registers, digests, trades,
          scores, jsons, eye-cards)
products/ ATLAS.html (+data), M5 lookup jsx
archive/  daily_ritual (the Daily Analyst future chassis + seed) · superseded ·
          unread_sources (original indicator Pine — UNREAD) · illustrations

## LAWS CARRIED
brief_state.zip (living daily state) is deliberately ABSENT — device copy only, by law.
History is append-only; vendor revisions are defects — detect, never absorb.
Dropped intermediates (touches, day summaries, board, bar diary, big ladder parquets)
regenerate from data/raw + code in minutes; recipes in docs/levelmap/REPRODUCTION_GUIDE.md
and docs/program/RUN_STATE.md.

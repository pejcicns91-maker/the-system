LEVEL MAP — FULL REPRODUCTION PACK (2026-07-09)
Layout: /data = RAW source (5m pickles x4, U history, witness) — everything derives from these.
/derived = the built layers for diffing + analysis (M1 state table, events, studies csvs, battery trades).
/docs = contracts (BINDING), manuals, every results doc, the Book, the registry, the F battery sheet+verdict.
/tools = daily emitter + both Pine scripts + parity cards.
/legacy = Option B (the live money system), brief engine + ritual, prior-program laws/graveyard, original Pine sources (see LEGACY_README).
START: read REPRODUCTION_GUIDE.md (definitions/constants/stages) -> run `python3 verify_results.py`
(13 assertions on the shipped layers; last run 13/13 PASS). To recreate from scratch: implement
Guide S1-S2 from /data, diff your events vs derived/level_events_v4.csv, then rebuild studies per
Guide S4 under the Triangle Protocol. Big regenerables (bar diary 505MB, approach matrix) are
spec-only per Guide S5. Laws & audit rights: docs/LEVEL_MAP_CONTRACT*.md — they bind any user of this pack.

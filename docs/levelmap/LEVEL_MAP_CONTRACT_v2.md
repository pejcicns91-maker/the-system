# LEVEL MAP — CONTRACT v2 (2026-07-09)
*Supersedes LEVEL_MAP_CONTRACT.md for scope; inherits its enforcement machinery verbatim (§3 research laws, §4 contractor failure modes F1–F9, §5 delivery protocol, §6 audit rights & quarantine, §9 auditor checklist). Chat instructions from Svet override this file; nothing else does. Any second AI given this file audits deliveries against it.*

## 0. STATE AT SIGNING
M1 unified state table ✓ (135,360 rows, accepted, spot-verified) · M2 constellation grid ✓ + M2b triples ✓ · M3 order grammar ✓ (order = minor axis, measured) · M4 indicators ✓ (Hayden gated 99.07%; H-Div ported, pending Svet's 20-row eye-card; volume blocked-on-source with banked 5.9y null prior; CB2 = renderer by design; CB4 source-requested) · M5 lookup artifact ✓ · THE_60PCT_BOOK ✓. Standing certified tier unchanged (thr×am table · contact layer · fast×after-trend 56.1 · weekend suppressor 35.6). Everything else possibility-tier.

## 1. THE TRIANGLE PROTOCOL (new binding law — applies to every future frozen/gated test)
Every gated test ships FOUR deliverables:
- **T1**: rules/cuts learned on H1 (≤2023-08-31) → judged on H2.
- **T2 (the mirror)**: learned on H2 → judged on H1.
- **T3**: learned on full history → judged on full history (the ceiling / production candidates).
- **T4 (the read)**: one table — same cells across T1/T2/T3, deltas explicit — plus the verdict per finding:
  · T1≈T2≈T3 → **era-proof**, strongest tier, T3 numbers go to production.
  · T3 ≫ both → overfit by that measured gap; finding demoted or narrowed.
  · T1 vs T2 asymmetric → the asymmetry NAMES the governing era; finding becomes a **regime marker**; production ships both era-versions, labeled, per the instability-is-signal law.
No frozen test may ship T1 alone again. Placebos and floors attach to every leg. UNDERPOWERED ≠ killed; tiny-n cells go to a labeled annex, never deleted.

## 2. COMMISSIONED WORK (in order; "Go" = next item; deferrals must be named PROPOSAL)
**M6 — Travel columns into the Book.** Every Book row gains "then how far": rejection travel, break carry-on, the live 0.6U mid-fight flip row, failed-crossing die-points — sizes attach per reaction-type (dials move odds, not sizes — banked), each convertible to $ via that day's U. Assembly of banked numbers + one census pass for any missing per-type size; no gate needed (descriptive), regime columns per law.
**M7 — Confluence: redundancy audit + the vote.** (a) Redundancy audit: correlation/overlap structure of all small-but-coherent dials (drive, RSI zone, intraday flip, knock lean, yday type, board density, wall composition when M8.5 lands); merge disguised duplicates; count DISTINCT votes. True-flat nulls (bin-free zeros like freshness-on-reaction) excluded by verdict, listed. (b) The unweighted vote: does aligned-vote-count move reaction odds monotonically? Full triangle protocol + placebo votes. (c) Regime weighting WITHOUT fitting: each dial's banked per-regime magnitude is its weight — the map weights itself; no fitted parameters. A fitted-weights model may be PROPOSED only after (b) passes.
**M8 — Biography × situation + the NAMED-SCENARIO REGISTRY.** Biography alone is memoryless (proven, stands). Test biography as a conditional: prior-break × thrust, freshness × constellation state, life-stage × regime, the parked C6 deep-break drift, W/M-proximity effects on daily levels (Svet's example — already live in M2 cells). Every surviving interaction gets a NAME (like PMPOC-recovery) and a registry entry: definition, odds, n, tier, date-list pointer. Triangle protocol on anything gate-bound.
**M8.5 — Dynamic zone-clusters + wall composition.** Recording upgrade: lines within a declared merge radius on a given day form one WALL object (radius = swept, not a single cut). Then the never-asked dial: wall COMPOSITION (contains a W/M member vs all-daily) on reaction odds — count-of-lines already banked flat, composition is the open axis. Fixes the coincident-line attribution defect from M3 as a side effect.
**M9 — The TradingView bridge (CB-M5).** Engine-side: an M5 payload line (zones-in-play, active Book cells for the live situation, travel budgets, scenario-registry hits) emitted daily/live from Python. Chart-side: an extended CB-family Pine renderer drawing it (CB2 grammar extended). Brain stays in Python; chart never holds stale tables. A self-contained Pine port of the live dials may be PROPOSED after, clearly labeled as sync-risk.
**Parked, unblocked by Svet**: div eye-card confirmation · CB4 source · volume Pine source · M2c (yday-archetype triples).

## 3. DELIVERY & AUDIT (inherited, restated)
Every delivery: mode + (b) line first · COVERAGE line · full-history + era + regime + asset + weekend columns, n on every % · audit pack (date lists csv, ≥4 real-data charts, tickets where trades exist) · honest-gaps section · next owed item named. Standing orders forever: "audit X × N", "draw me N from anything", "show the date list for {any number}". One failed eye-check = producing study QUARANTINED and rebuilt. Language law: nothing above its tier; "calibrated" list is exhaustive; possibility language elsewhere. No invented/synthesized probabilities anywhere, including in artifacts — measured cells only.

## 4. SIGNATURES
Principal: ____ (Svet — "signed" in chat freezes this verbatim; M6 starts on his Go)
Contractor: bound on signature; §0 status and inherited F1–F9 history acknowledged accurate.
Auditor: any AI holding this file; authority per inherited §5/§6/§9.

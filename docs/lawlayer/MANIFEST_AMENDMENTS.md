# PROJECT_HANDOFF_MANIFEST — LIVE AMENDMENT LOG
*Companion to PROJECT_HANDOFF_MANIFEST.md (which is otherwise frozen as assembled 2026-07-03). Amendments are appended here, never edited in place. A-1..A-4 accepted by Svet 2026-07-03.*

**A-1 (2026-07-03) — Extensionless old-corpus files re-uploaded as .txt.**
The 9 files the folder audit found absent (claude.ai rejected extensionless names) now live in knowledge as: Accurate_sd.txt, Claude_as_analyst.txt, Flux_sd.txt, Flux_sd2.txt, H-Div_forming.txt, Hayden_mtf_rsi.txt, New_B.txt, New_sd_instructions.txt, Volume_analysis_and_pivot.txt. **Treat NAME.txt as NAME everywhere** — all manifest/master-reference citations to the bare names resolve to the .txt twins. Old-corpus audit line "61 — ALL presented" is now satisfied via this mapping.

**A-2 (2026-07-03) — The two PDFs are covered by the capacity addendum, folder-only tier.**
MCO_Valuation_Screener.pdf and directional_analysis.pdf are NOT in project knowledge by design (capacity); they live in the user's folder (folder-only tier, same tier as brief_state.zip). Their text twins in knowledge carry the content: mco_terminal_map.md / mco_synthesis.md / mco_validation_study.md for the MCO screener; directional_analysis.md for the directional PDF. C_BRIDGE_MANUAL.pdf remains the only PDF in knowledge.

**A-3 (2026-07-03) — brief_engine_v3.py and sol_brief_engine_v2.py deleted from knowledge.**
Section G ("intentionally excluded") now matches the folder. Dead superseded copies — never reference them; the only engine is brief_engine_v4.py (v4.4 as of A-5).

**A-4 (2026-07-03) — Forward-ledger label/count drift: deferred to queue item 4.**
The Hayden five-vs-six enumeration, the "ETH-independence CMH" label (vs the pilot's "Bull-vs-pi CMH", since run and closed by the v1.2 evidence chain step 2), and the scattered battery-era items (I-1 gate, F-v1.2, SLR/HFiii) reconcile in FORWARD_REGISTER.md when queue item 4 executes. Until then the working count stands at 11–12 open claims.

---

**A-5 (2026-07-03) — Queue item 1 DELIVERED: Daily Pack v4.4** (this session).
Files superseded in knowledge on upload: brief_engine_v4.py (v4.3→v4.4), RUN_BRIEF.md, PACK_MANIFEST.txt. Scope: weekly U line (HORIZON-validated, crypto only; MONTH forecast excluded as registered) · PW/PM level map, all assets · REGIME light · S1 drift-guard with append-only caches · SIZE lines at the v1.2-locked 0.375 sizing · GER40 briefed via ^GDAXI [cash-hours] · CB2 payload extended append-safe (existing Pine unaffected). No trading rules changed.
Disclosures recorded with the delivery:
- **v4.3 defect found and fixed**: incremental fetch never completed the cache's boundary bar (frozen mid-bar at 08:00 ET), quietly degrading crypto PDH/PDL/PDC, ATR and DON-55 state detection; Yahoo merges absorbed vendor re-prints (keep-last), contrary to the S1 freeze-forward guarantee. v4.4 repairs the boundary bar each run and freezes settled history (detect-never-absorb). First v4.4 run on the living state shifts crypto levels slightly vs v4.3 output — expected and correct. Pack was 1 day old; no banked research numbers touched (research ran on fresh full fetches).
- **config.yaml staleness**: its base_risk_pct (0.75 challenge / 0.5 funded) is pre-lock history; the v1.2 sizing lock (0.375 uniform) governs, and the engine's SIZE lines implement the lock. config.yaml remains carried as an old-corpus base doc.
- Weekly implementation verified against an independent horizon-style recompute (exact match on the frozen forecasts; pooled walk-forward Spearman .526 vs the validated .527).

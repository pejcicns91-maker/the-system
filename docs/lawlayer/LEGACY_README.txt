LEGACY TIER — the pre-Level-Map operational & doctrinal core (from Svet's project).
Option B v1.2 = the ONLY validated money system (rulebook + bootstrap + trades). brief_engine_v4.py + RUN_BRIEF = the daily ritual the M9 emitter rides on (KNOWN DEFECT: expected v6.0 vs shipped v5.0 — unresolved; se_ref_vec.csv hash mismatch — unresolved; vendor law: detect, never absorb). 00_MASTER_REFERENCE + charter + handoff manifest = the prior program's laws and graveyard (binding as priors). Pine sources = the originals the M4 ports were gated against. NOT included: the full prior-study csv corpus — it lives in the project files, which remain the source of truth for anything not listed here.

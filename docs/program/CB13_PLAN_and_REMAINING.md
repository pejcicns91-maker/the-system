# CB13 — THE PLAN (loose, as requested · 2026-07-24)
CB13 = everything this week built, fused into one portable system: one zip, one ritual, any AI, zero tribal knowledge. The research harvest (map v3 + atlas + forecasters) is CB13's engine room, not a separate product. The research FACTORY (the GitHub runner grinding future waves) stays a separate machine that feeds upgrades into CB13 as version bumps.

## v1 — freeze what works (hours of build)
1. Rename/freeze the Pine pair: CB12.6 → **CB13** (chart layer: classed walls, cal%, slots, statuses, sentinel, alerts) · CB12.6a → **CB13a** (explainer pane). Version-locked; changes only by explicit bump.
2. **Atlas brain in the ritual:** atlas_cells.csv ships in the pack; the composing AI MUST cite certified cells on cards (rates + n) — vibes forbidden by protocol text.
3. **GitHub 08:00 safety net** on the Cb13 repo: a scheduled workflow emits a mechanical payload daily (walls + classes + cal% + rule-cards, no judgment words) so the chart is fed even on no-chat days. Chat brief remains the gold version.
4. Pack restructure to this archive's layout + START_HERE rewritten for CB13 semantics.

## v1.1 — the self-running chart
Self-computing Pine (walls, levels, classes, Hayden multi-TF computed inside TV — no paste needed for the floor layer) · live intensity meter + certified-cell lookups lighting as price approaches · CB-LIVE 30-min intraday tick (scenario tracking, rescoring with the approach-aware model, auto-recheck payloads committed to the repo).

## v1.2+ — research waves landing
W2 harvest (component trajectories + shape/divergence columns) → new certified cells + retrained B5 → cal% upgrade on cards · any money exit only via its own sealed prereg + independence test vs Option B (candidates C1–C4 queued).

## Governance (unchanged, binding)
CONTRACT_AUDIT + V3_CONTRACT ship inside · pre-authorization pattern for ritual AIs · append-only state · no code without explicit instruction · sealed gates for anything money.

---

# REMAINING — everything left to be done (nothing omitted)

## Pending actions (yours, small)
- **Send W2:** attach 06_W2/W2_SPEC.md + the paste block (06_W2/PASTE_BLOCK.txt) to the research chat. It executes end-to-end and hands you a grind.yml for the runner (GitHub dance you know).
- **b5 completion:** re-press the b5 task on GitHub until a run comes back green fast (bookmark-empty signal), then download the b5 model file from the repo and hand it to a chat to wire into the daily cards (upgrades cal%).
- grind.yml is currently finalize-only — the research chat hands you the restored dispatch version when W2 needs it (per D6).

## Research, optional (firewalled, no urgency)
- W2 execution (the spec is written; the biggest open knowledge gap — component histories/shapes were never crossed).
- b4 / t2-depth-4 ring (474,985 stacks, runner-days; outside the information frontier by decision #3).
- Full w1 per-station extraction (atlas currently carries the committed top-800 digest layer for w1).

## Money exits (each requires its own sealed prereg + independence vs Option B first)
- C1 virgin-break continuation · C2 wide-calm fade · C3 map-conditioned retest collection (the one gross-positive geometry, fee-blocked at 0.6U sizing) · C4 forecast-gated cards.

## Product builds (words-first, then code on instruction)
- CB13 v1 freeze (plan above) · self-computing Pine (12.6-solo) · CB-LIVE 30-min system · live intensity layer · 12.6a polish (scroll/pane ergonomics per your screenshots).

## Housekeeping / older open items (pre-existing, unchanged)
- dtype not logged by the brief engine (add to its log line so the PROVISIONAL-PORT hardens on sealed days) · R_A cost tier in ob_trades_S1 unreproduced (flagged, not gate-relevant) · se_ref_vec.csv hash mismatch · v5/v6 brief witness gap · parity eye-cards (H-Div, CB10) · parked proposals: M10 foreshadow, CB4 source, F2 signature, weekly trend-persistence integration, M9b full automation.

## Newly queued (2026-07-24 late)
- **W3 — corridor study (words-only, queued):** profile price IN TRANSIT between zones — origin→destination, duration, path shape (run/staircase/grind), mid-corridor stall points, multi-hop chains, completion vs abandonment — same event machinery, same ladder, same contracts. Fold into W2's runner phase or run after.
- **08:00 GitHub mechanical-payload net** (insurance for no-chat days) and **CB-LIVE event-driven emails** (only-on-change: floor breaks, primary flips, material cal% moves) — both parked pending Svet's word; designs discussed 2026-07-24.
- **b5 completion loop:** re-press task b5 on GitHub until a fast-green run (bookmark empty), then download the b5 model file → wire per 06_PLANS_AND_INCOMING/INCOMING_FINDINGS_PROTOCOL.md.

## The daily ritual (unchanged, forever)
Attach 01_DAILY_SYSTEM as a zip (or SVET_DAILY_RUN.zip) → "run the brief" → payload into both Pine boxes → returned zips are tomorrow's state. Audit phrase: "AUDIT: prove it — full contract." Scope phrase: "V3 CHECK."

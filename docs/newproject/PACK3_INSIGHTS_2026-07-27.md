# PACK 3 INSIGHTS — SVET_FULL_PACK_2026-07-22 (the level-map program, found)
Read this session: every doc/contract/results file to EOF (outer + repro pack), CB9/CB10 Pine in full, reproduction guide + verify harness, Option B rulebooks v1.0/1.1/1.2, master reference, forward register, intraday verdict, horizon results, bootstraps, payload examples; all data layers programmatically verified (M1_state 135,360×70 schema listed; battery 186,208 ✓; m2_grid 3,456 ✓; m4_columns 45,481 ✓; raw SOL pickle 509,720 bars 2021-09→2026-07-06). PDFs probed and identified as rendered versions of the read manuals. Declared unread (nothing blocks; required reading the day their components go IN): legacy Pine sources (Hayden_mtf_rsi, SOL_AB, New_B, cbridge_cb2, H-Div), CB11/CB12.2/2b renderer iterations, M5 jsx internals, playbook PDFs page-by-page, brief_state_SEED contents.

## 1. SECTION-B MYSTERIES — ALL RESOLVED (definitions now in hand)
- **thrust (thr30)**: signed 30-min displacement toward the level at touch, in U. Terciles ≤0.0999 rev / ≤0.2303 slow / else fast (contest-frame variant 0.109/0.251).
- **drive24**: signed ~22.75h (273-bar) displacement toward the level, in U. ≤−0.0268 calm / ≤0.4959 mid / else driven.
- **wear (am20)**: count of the last 20 window-days whose range straddled the level. 0–2 few / 3–7 mid / 8+ many. days_since_touch>20 = virgin.
- **vote (M7)**: 9 dials (thr, drv, am, yday-type, weekday, 4H-regime, flip, RSI-side, density), each ±1/0 by the sign of its full-history break deviation at a ±1pp floor; redundancy-audited (max pair corr 0.35); regime abstains (under floor) and conditions instead. 3+ reject-aligned → 67.4% reject [TRI, placebo z≈22].
- **escalation ladder**: P(day ends through | max pen so far ≥x): .2→59.6 .3→65.7 .4→71.1 .5→75.6 .6→79.2 .8→84.8 1.0→88.6 — era-proof ±1–2pp; a smooth escalator, no cliff.
- **archetypes**: k-means k=6/10/14 on the ACTUAL 288-bar close paths, three frames (U-from-open / % / 0–1 squeeze); daypaths.npz holds the raw paths for 6,764 coin-days; yd map k10 {0 lean,1–2 churn,3 trend,4 parked}.
- **density/constellation**: per-level local density counts + the 12-state grammar (side × 5-day cross × freshness) per level → the M2 grid (3,456 cells; POC cascades ±19pp, weekly-close suppressor −14pp).
- **walls (M8.5)**: merge level values at r∈{0.15,0.25,0.35}·U — a SWEPT radius (0.25 canonical). Composition (WM member) = NULL once any-of-N counting was corrected.
- **zone band**: level ± 0.04·uU half-width (recorder constant). Outcomes: brk = pen>0.60U (a DISCOVERED boundary, 0.57–0.59 verified, weekend 0.51); cont = exit-side continuation.
- **CB4**: resolved — no CB4 source exists anywhere; the "manual" was CB2 screenshots; CB2/CB4 are renderers computing zero signals (matches Svet: all CBxx = the TradingView bridge). Clause closed by identification.
- **Option B rulebooks**: v1.0 (Hayden Master Book), v1.1 (Option B full contract), v1.2 (DON-55 skip rule + locked 0.375 sizing + evidence chain) — all now in hand, incl. FTMO specs, ladder, edge cases.

## 2. CORRECTIONS TO THE FIDELITY AUDIT (rule 9 applied to my own work)
1. **"No per-bar record ever existed" — half wrong.** The level-map program's recorder WAS a bar-level diary: 38.97M rows = every 5m bar × every level, zero-gap, parity-proven. It stored bar×level geometry (not component readings per bar), was pack-excluded at 505MB as spec-only regenerable, and got consumed into level-day rows (M1) for all analysis. Standing truth: a per-bar PRICE×LEVEL record existed once; per-bar COMPONENT readings still never have.
2. **"0.6U frozen constant" — origin corrected.** In the level-map program 0.6U was discovered from the pen distribution (and shown to flex: weekend 0.51) and the ladder proves it's no cliff — just where through-odds cross ~80%. The later bruteforce/money program then froze that READING statistic into a stop/R-unit. The category error stands; the constant's origin is data.
3. **Parameters WERE swept here**: wall radius (3 values), hover radius (grid 0.05–0.5U, "the finding is a curve, not a category"), episode ruler k=1–24, archetype k=6/10/14, three path frames. The frozen-parameter disease is concentrated in the money tests and the bruteforce wave, not uniform across the history.
4. **The strongest anti-collapse machinery already existed**: LEVEL_MAP_CONTRACT F1–F9 (F1 scope-shrinking with named past breaches — touch-only recording, 2 of 288 horizons, folded direction, dropped indicator clause; F2 example-anchoring; F3 threshold-inventing; F8 aggregate-hiding; F9 fatigue-signing), the Triangle Protocol, standing audit rights ("show the date list", "draw me N", one failed eye-check = quarantine). The new project's rules 1–10 are the same law written a second time — merge the F-list in as the historical case file.

## 3. THE ANALYST'S DIRECT ANCESTOR — NAMED, NEVER BUILT
DAYPATH_ARCHETYPES_RESULTS, verbatim: "**partial-path matching** — at 11:00, match today's path-so-far to the archetype library and inherit the remainder's statistics (the named next step, not yet built)." That is the Daily Analyst's core move, specified a month ago. The raw material exists (daypaths.npz, real 288-bar paths) — the only place raw paths were ever kept. C1 (level-relative path families, k=7) and C5 (board-journey families, k=5) are the through-lines' closest ancestors — both clustered REAL paths, both stopped at day grain without component readings.

## 4. MARGINAL GAINS — ALREADY CONFIRMED AT SNAPSHOT GRAIN
M7's verdict, verbatim: "Aggregation of marginal gains is real on this data — repeatable, not guaranteed, exactly as you framed it." Nine small dials, none certified alone → 67.4% [TRI]. MINT/MINT2/MAP_V2 then proved the map trend-orthogonal, Option-B-independent, and day-type the strongest single conditioner (TRI +22.5), with the two real interactions logged (EXPANSION overrides the brakes; QUIET amplifies the holds). The System's architecture bet has a proven precedent; the new project extends it from snapshots to sequences.

## 5. U — THE WARNING SHARPENS
Two DIFFERENT U definitions exist across programs: level-map U = per-coin day-move unit % from the brief engine's KNN reference (se_ref_uhist); bruteforce uAbs = median of the last 14 daily ranges. "U" numbers quoted across programs are not the same ruler. And MINT2 already flagged the denominator artifact honestly (a size forecast partly forecasts the U denominator) and proved behavior beyond it via exit-side continuation (+12/−6.8, not U-denominated) — the exact two-ruler vigilance rule 10 now mandates, practiced once and forgotten.

## 6. WHAT THE PACK ADDS TO THE BUILD
Raw 5m pickles ×4 (2021-09→2026-07-06) ship IN the repro pack — the new base layer's data seed already in hand, hash-drift law attached. M1_state's 70-column schema is a proven conditioning-column checklist to port to per-bar grain. verify_results.py's 13-assertion pattern = the acceptance-harness template for the new record. The toll table (sub-1% brackets need 57–72% path win rates; humane at 2%+ captures; venue = a third of FTMO friction on spot-maker) is the standing money-side constraint every future geometry faces.
